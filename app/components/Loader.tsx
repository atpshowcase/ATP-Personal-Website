import { ClipLoader, PacmanLoader, HashLoader } from "react-spinners";

const MyComponent = () => {
  return (
    <div>
      <HashLoader size={80} color={"black"} />
    </div>
  );
};

export default MyComponent;
